spring:
    datasource:
        url: jdbc:h2:mem:h2db
        username: main
        password: password
        driverClassName: org.h2.Driver
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
    cache:
        type: simple
        cache-names: genres